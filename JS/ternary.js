// let mark=40;

// result= mark>35 ? "pass":"failed";


// console.log(result)

// let eng,marathi,hindi;

// eng=4;
// marathi=65;
// hindi=97;

// result= eng>35 && marathi>35 && hindi>35 ? "pass" : "failed";

// console.log(result)

/*id="pan"
entry= id=="adhar" || id=="pan" || id=="passport" ? "allowed" : "not allowed"
console.log(entry)*/

/*
1. 90 - A
2. 80 - B 
3. 70 - C 
4. 40 - D 
5. failed*/

mark=35;

result= mark>90 ? "a":mark>80 ? "B" : mark>70 ?"C": mark>40 ?"D": "failed"
console.log(result)